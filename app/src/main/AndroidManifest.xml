<?xml version="1.0" encoding="utf-8"?>
<manifest package="isel.pdm.yamda"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- /Permissions -->

    <application
        android:name=".YamdaApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- Activities -->
        <activity
            android:name=".ui.activity.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <meta-data
                android:name="android.app.default_searchable"
                android:value=".ui.activity.SearchableActivity"/>
        </activity>

        <activity android:name=".ui.activity.SearchableActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH"/>
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"/>
        </activity>

        <activity
            android:name=".ui.activity.MovieActivity"
            android:label="@string/app_name">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.activity.HomeActivity"/>
        </activity>

        <activity
            android:name=".ui.activity.AboutActivity"
            android:label="@string/about"/>

        <activity
            android:name=".ui.activity.PreferencesActivity"
            android:label="@string/preferences"/>
        <!-- /Activities -->

        <!-- Receivers -->
        <receiver android:name=".data.handlers.NotificationPublisher"/>
        <!-- /Receivers -->

        <!-- Services -->
        <service
            android:name=".data.services.MovieDetailsService"
            android:exported="false"/>
        <service
            android:name=".data.services.lists.TheatersListService"
            android:exported="false"/>
        <service
            android:name=".data.services.lists.TopListService"
            android:exported="false"/>
        <service
            android:name=".data.services.lists.SoonListService"
            android:exported="false"/>
        <service
            android:name=".data.services.MovieSearchService"
            android:exported="false"/>
        <!-- /Services -->

        <!-- Provider -->
        <provider
            android:name=".data.provider.MoviesContentProvider"
            android:authorities="isel.pdm.yamda.provider"
            android:exported="false"/>
        <!-- /Provider -->

    </application>

</manifest>
